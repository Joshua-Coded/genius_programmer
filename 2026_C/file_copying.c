#include <stdio.h>

int main () {
    // int c;

    // c = getchar();
    // while (c != EOF) {
    //     putchar(c);
    //     c = getchar();
    // }

    // int c;

    // while ((c = getchar()) != EOF)
    //     putchar(c);
    int c;
    // c = (getchar() != EOF);

    // printf("%d", c);

    printf("EOF is %d\n", EOF);
}