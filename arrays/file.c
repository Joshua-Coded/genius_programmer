/* one way of initialising array*/

// int iArray[10] = {1,2,3,4,5};

/* another way of initialising the  array using for loop*/

#include <stdio.h>

int main () {
    int x;
    int iArray[5] = {1,2,3,4,5};

// initialise array elements
    // for (x = 0; x < 5; x++)
    //     iArray[x] = x;
    

// print array elements contents
    for (x = 0; x <= iArray[x]; x++)
        printf("\nThe value of iArray index %d is %d\n", x, x);


}
